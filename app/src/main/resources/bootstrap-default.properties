# Switch to pem key for local testing only
jsk.spring.config.idps.connection.policyId=
# Must be specified as env var
jsk.spring.config.idps.connection.apiKeyId=${IDPS_API_KEY}
jsk.spring.config.idps.connection.apiSecretKey=${IDPS_API_KEY_PATH}
